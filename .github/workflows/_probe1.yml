name: probe1
on:
  workflow_dispatch:
    inputs:
      hostname:
        description: 'x'
        required: false
        default: ''
      keep_alive_minutes:
        description: 'x'
        required: false
        default: '355'
run-name: 'probe1 ${{ github.run_id }}'
defaults:
  run:
    shell: bash
jobs:
  p:
    runs-on: macos-latest
    timeout-minutes: 360
    env:
      CRD_CODE: ${{ secrets.CRD_CODE }}
    steps:
      - name: v
        env:
          H: ${{ inputs.hostname }}
          K: ${{ inputs.keep_alive_minutes }}
        run: echo ok